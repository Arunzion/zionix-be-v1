apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - admin-service.yaml
  - auth-service.yaml
  - postgres.yaml
  - kafka.yaml
  - redis.yaml
  - service-mesh.yaml

commonLabels:
  app.kubernetes.io/part-of: zionix-platform
  app.kubernetes.io/managed-by: kustomize
